---
phase: 07-ambient-retention-of-csinh3-class-phases
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-barrier-and-instability-map.md
  - .gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-barrier-and-instability-map.json
interactive: false

conventions:
  units: "eV/f.u., GPa, cm^-1"
  barrier_bins: "barrierless or near-zero | low | moderate | high"
  path_types: "soft-mode distortion | decomposition route | method-limited proxy"
  method_rule: "use fixed-cell CI-NEB only when endpoint compatibility justifies it; otherwise state the cell-coupling limitation explicitly"

dimensional_check:
  barrier_height: "[eV/f.u.]"
  pressure: "[GPa]"
  omega_min: "[cm^-1]"

approximations:
  - name: "Representative-path barrier"
    parameter: "one dominant instability branch is used as the main quenchability proxy"
    validity: "phase-level verdict on whether retention is plausible at all"
    breaks_when: "multiple unrelated low-barrier branches exist and the chosen route is not the easiest failure path"
    check: "justify endpoint selection from the Plan 07-01 failure classification"
  - name: "Cell-compatible barrier method"
    parameter: "QE CI-NEB is used only when endpoints are cell-compatible; otherwise a solid-state or proxy route is reported with limitation"
    validity: "qualitative barrier ranking and metastability triage"
    breaks_when: "the dominant transition requires large coupled strain and the method hides it"
    check: "state whether the chosen method includes or excludes cell degrees of freedom"

estimated_execution:
  total_minutes: 60
  breakdown:
    - task: 1
      minutes: 20
      note: "select the dominant failure branch and endpoints"
    - task: 2
      minutes: 25
      note: "estimate a representative barrier and encode method limitations"
    - task: 3
      minutes: 15
      note: "translate the barrier into quenchability semantics"

contract:
  schema_version: 1
  scope:
    question: "Does the dominant CsInH3 decompression failure branch carry a meaningful barrier, or is pressure release effectively barrierless?"
  claims:
    - id: claim-csinh3-barrier-route
      statement: "At least one explicit CsInH3 instability or decomposition route can be defined and assigned a barrier-aware verdict rather than being described hand-wavily."
      deliverables: [deliv-csinh3-barrier-map, deliv-csinh3-barrier-json]
      acceptance_tests: [test-endpoint-consistency, test-barrier-method-appropriateness, test-no-energy-difference-proxy]
      references: [ref-plan01-output, ref-v1-conclusions, ref-qe-neb, ref-ssneb]
    - id: claim-csinh3-quench-signal
      statement: "The representative barrier can be translated into a planning-grade quenchability signal for CsInH3 without overclaiming retained superconductivity."
      deliverables: [deliv-csinh3-barrier-map]
      acceptance_tests: [test-quench-signal-class, test-no-analog-overreach]
      references: [ref-plan01-output, ref-hg1223-quench, ref-ssneb]
  deliverables:
    - id: deliv-csinh3-barrier-map
      kind: report
      path: ".gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-barrier-and-instability-map.md"
      description: "Barrier-aware route map for the dominant CsInH3 decompression failure branch"
      must_contain:
        - "initial and final endpoints"
        - "route type"
        - "barrier value or explicit barrierless verdict"
        - "method limitation note"
        - "quenchability interpretation"
    - id: deliv-csinh3-barrier-json
      kind: dataset
      path: ".gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-barrier-and-instability-map.json"
      description: "Machine-readable endpoint, method, and barrier data for the CsInH3 failure route"
      must_contain:
        - "pressure_gpa"
        - "route_type"
        - "initial_state"
        - "final_state"
        - "barrier_eV_fu"
        - "method"
        - "interpretation"
  references:
    - id: ref-plan01-output
      kind: prior_artifact
      locator: ".gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-decompression-path.md"
      aliases: ["phase07-plan01-output"]
      role: benchmark
      why_it_matters: "Defines the first failure interval and the dominant branch that this plan must follow."
      applies_to: [claim-csinh3-barrier-route, claim-csinh3-quench-signal]
      carry_forward_to: [planning, verification, writing]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-v1-conclusions
      kind: prior_artifact
      locator: "data/project_conclusions.md"
      aliases: ["repo-v1-conclusions"]
      role: benchmark
      why_it_matters: "Carries the warning that quenchability is unknown and must not be inferred from low-pressure Tc alone."
      applies_to: [claim-csinh3-barrier-route]
      carry_forward_to: [verification, writing]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-qe-neb
      kind: spec
      locator: "Quantum ESPRESSO neb.x input description"
      aliases: ["qe-neb-docs"]
      role: method
      why_it_matters: "Defines the supported CI-NEB workflow and its input assumptions."
      applies_to: [claim-csinh3-barrier-route]
      carry_forward_to: [planning, verification]
      must_surface: true
      required_actions: [read, use, cite]
    - id: ref-ssneb
      kind: paper
      locator: "Sheppard, Terrell, and Henkelman, J. Chem. Phys. 136, 074103 (2012)"
      aliases: ["ssneb-2012"]
      role: method
      why_it_matters: "Provides the cell-aware NEB logic needed when strain and atomic motion are coupled."
      applies_to: [claim-csinh3-barrier-route, claim-csinh3-quench-signal]
      carry_forward_to: [planning, verification, writing]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-hg1223-quench
      kind: paper
      locator: "Deng et al., PNAS 123, e2536178123 (2026)"
      aliases: ["hg1223-quench-2026"]
      role: background
      why_it_matters: "Shows that pressure-quench retention is a real route class, while still not validating hydride retention automatically."
      applies_to: [claim-csinh3-quench-signal]
      carry_forward_to: [verification, writing]
      must_surface: true
      required_actions: [read, compare, cite]
  acceptance_tests:
    - id: test-endpoint-consistency
      subject: claim-csinh3-barrier-route
      kind: consistency
      procedure: "Verify that the initial and final states used for the barrier route match the dominant failure branch identified in Plan 07-01."
      pass_condition: "The barrier route is not detached from the actual first failure mode."
      evidence_required: [deliv-csinh3-barrier-map, ref-plan01-output]
      automation: hybrid
    - id: test-barrier-method-appropriateness
      subject: claim-csinh3-barrier-route
      kind: consistency
      procedure: "Inspect the method note and verify that the chosen barrier approach matches the endpoint geometry and cell-change requirements."
      pass_condition: "The report either uses a compatible NEB-style method or states clearly why the result is a proxy."
      evidence_required: [deliv-csinh3-barrier-map, ref-qe-neb, ref-ssneb]
      automation: hybrid
    - id: test-no-energy-difference-proxy
      subject: claim-csinh3-barrier-route
      kind: proxy
      procedure: "Check that the report does not treat a final-state energy difference as the kinetic barrier."
      pass_condition: "Barrier values and endpoint energy differences are reported distinctly."
      evidence_required: [deliv-csinh3-barrier-map, deliv-csinh3-barrier-json]
      automation: automated
    - id: test-quench-signal-class
      subject: claim-csinh3-quench-signal
      kind: consistency
      procedure: "Inspect the interpretation block and verify that the barrier result is translated into a bounded planning verdict."
      pass_condition: "The route is labeled barrierless or near-zero, low, moderate, or high, with a corresponding quenchability note."
      evidence_required: [deliv-csinh3-barrier-map]
      automation: automated
    - id: test-no-analog-overreach
      subject: claim-csinh3-quench-signal
      kind: proxy
      procedure: "Check that the report uses Hg1223 only as an analog route class, not as proof that hydrides quench successfully."
      pass_condition: "The analog is explicitly limited and does not substitute for the CsInH3 barrier evidence."
      evidence_required: [deliv-csinh3-barrier-map, ref-hg1223-quench]
      automation: hybrid
  forbidden_proxies:
    - id: fp-energy-difference-as-barrier
      subject: claim-csinh3-barrier-route
      proxy: "Calling the energy difference between endpoints the activation barrier"
      reason: "Thermodynamic driving force is not a kinetic path."
    - id: fp-fixed-cell-neb-without-warning
      subject: claim-csinh3-barrier-route
      proxy: "Using a fixed-cell path for a cell-driven transition without stating the limitation"
      reason: "The computed route could miss the true easy-collapse channel."
    - id: fp-hg1223-proves-hydride-quench
      subject: claim-csinh3-quench-signal
      proxy: "Treating the Hg1223 pressure-quench result as evidence that CsInH3 must also retain"
      reason: "Analog success does not transfer automatically across materials classes."
  links:
    - id: link-barrier-map
      source: claim-csinh3-barrier-route
      target: deliv-csinh3-barrier-map
      relation: supports
      verified_by: [test-endpoint-consistency, test-barrier-method-appropriateness, test-no-energy-difference-proxy]
    - id: link-barrier-json
      source: claim-csinh3-barrier-route
      target: deliv-csinh3-barrier-json
      relation: supports
      verified_by: [test-no-energy-difference-proxy]
    - id: link-quench-signal
      source: claim-csinh3-quench-signal
      target: deliv-csinh3-barrier-map
      relation: supports
      verified_by: [test-quench-signal-class, test-no-analog-overreach]
  uncertainty_markers:
    weakest_anchors:
      - "The easiest collapse route could differ from the representative route chosen here."
      - "A fixed-cell proxy can only bound, not fully resolve, a strongly strain-coupled transition."
    disconfirming_observations:
      - "An alternate route has a clearly lower barrier than the one modeled here."
      - "A cell-aware calculation removes the apparent barrier entirely."

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made:
    - "Forced route-method compatibility checks so the barrier section cannot collapse into a hand-wavy metastability note."
---

<objective>
Estimate a representative barrier for the dominant `CsInH3` decompression failure branch and translate it into a planning-grade quenchability signal.

Purpose: This plan is the difference between a vague metastability discussion and an actual quenchability verdict. It determines whether pressure release is plausibly bottlenecked by a barrier or whether collapse is effectively immediate.
Output: A route map and JSON dataset with endpoints, method choice, barrier values or bounds, and an explicit quenchability interpretation.
</objective>

<execution_context>
@./.codex/get-physics-done/workflows/execute-plan.md
@./.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/07-ambient-retention-of-csinh3-class-phases/07-RESEARCH.md
@.gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-decompression-path.md
@data/project_conclusions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select the dominant CsInH3 failure branch and define endpoints</name>
  <files>.gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-barrier-and-instability-map.md, .gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-barrier-and-instability-map.json</files>
  <action>
    Read the decompression output from Plan `07-01` and determine the dominant failure branch.

    Endpoint rules:
    - if the first failure is a soft-mode distortion, construct a lower-symmetry endpoint by following the unstable mode
    - if the first failure is thermodynamic decomposition, choose the nearest relevant product set from the hull analysis
    - if both appear together, pick the lower-complexity route first but document the competing branch explicitly

    Record the chosen initial and final states, the pressure at which the route is evaluated, and why this route is the right representative branch.
  </action>
  <verify>
    1. The route follows the first actual failure mode from Plan `07-01`.
    2. Endpoint definitions are explicit enough to support a barrier path.
    3. Competing routes are at least named if they could matter.
  </verify>
  <done>The barrier map defines a specific route with named endpoints and a clear reason for choosing it.</done>
</task>

<task type="auto">
  <name>Task 2: Choose the correct barrier method and estimate the route barrier</name>
  <files>.gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-barrier-and-instability-map.md, .gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-barrier-and-instability-map.json</files>
  <action>
    Match the route to the right barrier method.

    Method hierarchy:
    1. QE `neb.x` / CI-NEB for cell-compatible endpoints
    2. generalized solid-state NEB logic when cell relaxation is part of the path
    3. a documented proxy route only if the first two are not yet executable in the current setup

    Report:
    - method actually used
    - whether the cell is allowed to change
    - barrier in `eV/f.u.` or an explicit barrierless / near-zero verdict
    - distinction between endpoint energy difference and activation barrier
  </action>
  <verify>
    1. The method note matches the endpoint geometry.
    2. No barrier is inferred from endpoint energetics alone.
    3. The barrier result is numerical where possible and otherwise bounded qualitatively with a clear reason.
  </verify>
  <done>The route map contains a barrier estimate or explicit barrierless result with a method limitation note.</done>
</task>

<task type="auto">
  <name>Task 3: Convert the barrier result into a bounded quenchability signal</name>
  <files>.gpd/phases/07-ambient-retention-of-csinh3-class-phases/csinh3-barrier-and-instability-map.md</files>
  <action>
    Interpret the barrier in the context of pressure release.

    Required output:
    - classify the barrier as `barrierless or near-zero`, `low`, `moderate`, or `high`
    - explain what that means for a pressure-quenched CsInH3 phase
    - note explicitly that this is a planning-grade signal, not proof of retained superconductivity
    - limit the use of the Hg1223 analog to route-class motivation only
  </action>
  <verify>
    1. The interpretation stays inside the allowed verdict language.
    2. The report does not over-extend the pressure-quench analog literature.
    3. The result is ready to feed the final Phase `07` scorecard.
  </verify>
  <done>The report ends with a bounded quenchability signal that can be combined with derivative evidence in Plan `07-03`.</done>
</task>

</tasks>

<verification>
- Check that barrier and endpoint-energy quantities are never conflated.
- Check that the route definition still reflects the first failure mode from Plan `07-01`.
- Check that analog pressure-quench papers are treated as context, not evidence replacement.
</verification>

<success_criteria>
- A specific CsInH3 failure route is defined and evaluated.
- The chosen barrier method is appropriate for the path geometry or is labeled explicitly as a proxy.
- The output provides a bounded quenchability signal without claiming experimental retention.
</success_criteria>

<output>
After completion, create `.gpd/phases/07-ambient-retention-of-csinh3-class-phases/07-02-SUMMARY.md`.
</output>
